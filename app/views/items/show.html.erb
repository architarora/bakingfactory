<% if can? :edit, @active_items %>
<%= render partial: "partials/structure2", locals: {model_name: "item", 
                                                         object: @item,
                                                         primary: "item_details", 
                                                         secondary: nil,
                                                         sidebar: "partials/previous_prices"} %>
<% else %>

<%= render partial: "partials/structure2", locals: {model_name: "item", 
                                                         object: @item,
                                                         primary: "item_details", 
                                                         secondary: nil,
                                                         sidebar: "recommended_items"} %>
<% end %>